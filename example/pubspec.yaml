name: courier_map_tracker_example
description: Example application demonstrating the courier_map_tracker package.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.13.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  courier_map_tracker:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
